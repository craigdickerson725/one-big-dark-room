from django.apps import AppConfig

# Configuration class for the BandListing application
class BandListingConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'band_listing'
